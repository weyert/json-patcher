{
    "name": "diff-updater",
    "version": "0.1.0",
    "description": "Diff Updater",
    "main": "node/index.js",
    "browser": "artifacts/browser.mjs",
    "types": "types.d.ts",
    "repository": "https://github.com/formbird/diff-updater",
    "exclude": [
        "target/*",
        "artifacts/*"
    ],
    "scripts": {
        "node:build": "cargo-cp-artifact -nc node/diff_updater.node -- cargo build --message-format=json-render-diagnostics",
        "node:build:release": "npm run node:build -- --release",
        "build:vite": "vite build",
        "browser:build": "cargo build --target wasm32-unknown-unknown && wasm-bindgen --out-dir browser/ --target bundler target/wasm32-unknown-unknown/debug/diff_updater.wasm && npm run build:vite",
        "browser:build:release": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --out-dir artifacts/ --target bundler target/wasm32-unknown-unknown/release/diff_updater.wasm && npm run build:vite",
        "build": "npm run node:build && npm run browser:build",
        "build:release": "npm run node:build:release && npm run browser:build:release",
        "install": "npm run node:build:release",
        "pretest": "tsc -p test/tsconfig.json",
        "test": "ava"
    },
    "license": "",
    "devDependencies": {
        "@types/node": "^20.1.5",
        "ava": "^5.2.0",
        "cargo-cp-artifact": "^0.1",
        "rfc6902": "^5.0.1",
        "typescript": "^5.0.4",
        "vite": "^4.2.2",
        "vite-plugin-top-level-await": "^1.3.0",
        "vite-plugin-wasm": "^3.2.2"
    }
}
